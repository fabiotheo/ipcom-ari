export interface AriClientConfig {
    host: string;
    port: number;
    username: string;
    password: string;
    secure?: boolean;
}
export interface AriApplication {
    name: string;
}
//# sourceMappingURL=requests.d.ts.map